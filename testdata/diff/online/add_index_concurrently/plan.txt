Plan: 1 to modify.

Summary by type:
  tables: 1 to modify

Tables:
  ~ users
    + idx_users_email_status (index)

DDL to be executed:
--------------------------------------------------

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_users_email_status ON users (email, status);
