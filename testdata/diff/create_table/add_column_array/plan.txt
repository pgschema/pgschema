Plan: 1 to modify.

Summary by type:
  tables: 1 to modify

Tables:
  ~ articles
    + statuses (column)
    + tags (column)

DDL to be executed:
--------------------------------------------------

ALTER TABLE articles ADD COLUMN tags text[];

ALTER TABLE articles ADD COLUMN statuses status[];
