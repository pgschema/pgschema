Plan: 1 to modify.

Summary by type:
  tables: 1 to modify

Tables:
  ~ documents
    + metadata (column)

DDL to be executed:
--------------------------------------------------

ALTER TABLE documents ADD COLUMN metadata jsonb;
