Plan: 1 to add.

Summary by type:
  tables: 1 to add

Tables:
  + users

DDL to be executed:
--------------------------------------------------

CREATE TABLE IF NOT EXISTS users (
    id SERIAL,
    name text,
    email text,
    CONSTRAINT users_pkey PRIMARY KEY (id)
);
