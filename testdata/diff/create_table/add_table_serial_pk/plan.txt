Plan: 1 to add.

Summary by type:
  tables: 1 to add

Tables:
  + users

DDL to be executed:
--------------------------------------------------

-- Transaction Group #1
CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    name text,
    email text
);
