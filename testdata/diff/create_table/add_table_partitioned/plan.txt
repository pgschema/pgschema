Plan: 1 to add.

Summary by type:
  tables: 1 to add

Tables:
  + orders

DDL to be executed:
--------------------------------------------------

CREATE TABLE IF NOT EXISTS orders (
    id integer NOT NULL,
    order_date date NOT NULL,
    customer_id integer
);
