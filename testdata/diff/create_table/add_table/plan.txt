Plan: 1 to add.

Summary by type:
  tables: 1 to add

Tables:
  + departments

DDL to be executed:
--------------------------------------------------

CREATE TABLE IF NOT EXISTS departments (
    id integer,
    name text NOT NULL,
    CONSTRAINT departments_pkey PRIMARY KEY (id)
);
