-- Initial default privileges
ALTER DEFAULT PRIVILEGES GRANT SELECT ON TABLES TO app_user;
ALTER DEFAULT PRIVILEGES GRANT USAGE ON SEQUENCES TO app_user;

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);
