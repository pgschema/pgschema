-- Default privileges configured
ALTER DEFAULT PRIVILEGES GRANT SELECT ON TABLES TO readonly_user;
ALTER DEFAULT PRIVILEGES GRANT INSERT, UPDATE, DELETE ON TABLES TO app_user;

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL
);
