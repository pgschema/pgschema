Plan: 1 to modify.

Summary by type:
  tables: 1 to modify

Tables:
  ~ users
    + idx_users_fullname_search (index)

DDL to be executed:
--------------------------------------------------

-- Transaction Group #1
CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_users_fullname_search ON users (lower(first_name), lower(last_name), lower(email));
