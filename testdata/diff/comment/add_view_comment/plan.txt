Plan: 1 to modify.

Summary by type:
  views: 1 to modify

Views:
  ~ employee_view
    + employee_view (comment)

DDL to be executed:
--------------------------------------------------

CREATE OR REPLACE VIEW employee_view AS
SELECT 
    e.id,
    e.name,
    e.department,
    e.salary
FROM public.employees e
WHERE e.active = true;

COMMENT ON VIEW employee_view IS 'Shows all active employees';
