Plan: 1 to modify.

Summary by type:
  views: 1 to modify

Views:
  ~ active_employees

DDL to be executed:
--------------------------------------------------

-- Transaction Group #1
CREATE OR REPLACE VIEW active_employees AS
 SELECT
    id,
    name,
    salary,
    status
   FROM employees
  WHERE status = 'active';
