Plan: 1 to add.

Summary by type:
  materialized views: 1 to add

Materialized views:
  + active_employees

DDL to be executed:
--------------------------------------------------

CREATE MATERIALIZED VIEW IF NOT EXISTS active_employees AS
SELECT 
    id,
    name,
    salary
FROM employees
WHERE status = 'active';
